/* ============================================================
   config.js — runtime config.
   Set SYNC_URL to your deployed sync Worker to turn ON cross-device
   sync. Leave it empty ('') and the app runs single-device, exactly
   as before (no backend, no external requests).
   e.g. SYNC_URL: 'https://otakuchore-sync.yourname.workers.dev'
   ============================================================ */
window.CT_CONFIG = {
  SYNC_URL: 'https://otakuchore-sync.neeraj-it3-uk.workers.dev'
};
